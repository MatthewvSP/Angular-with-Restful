# Angular-with-Restful

To start the angular app go to '/traderApp' and type the command 'ng serve' into the commandline.

To start the node server backend go to the root directory and type the command 'node api.js' into the commandline.
